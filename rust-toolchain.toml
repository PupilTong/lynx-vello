[toolchain]
# Latest nightly (repo developed against rustc 1.98.0-nightly, 2026-07-01).
channel = "nightly"
components = ["rustfmt", "clippy", "rust-analyzer", "rust-src"]
targets = ["wasm32-unknown-unknown"]
profile = "default"
