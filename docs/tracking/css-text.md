# CSS text & font properties

> Status: pending initial research pass — see [README.md](README.md).

Will cover: `font-family`/`size`/`weight`/`style`, `line-height`,
`letter-spacing`/`word-spacing`, `text-align`, `text-overflow`, `white-space`,
`text-decoration`, `text-shadow`, text-stroke (`-x-text-stroke` or similar),
`direction`/`writing-mode`, `text-indent`, `word-break`/`overflow-wrap`,
line-clamp (`-x-line-clamp`/`-webkit-line-clamp`), `vertical-align`, and any
Lynx-specific text measurement/line-breaking quirks a `parley`-based
implementation needs to replicate for compatibility.

Scope note: this is the spec for the `parley` integration — see
`.claude/agents/lynx-text-engine.md`.
