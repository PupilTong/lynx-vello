# CSS text & font properties

> Research: multi-agent sweep over `lynx/` and `lynx-stack/` (see [AGENTS.md](../../AGENTS.md) for the reference-repo shorthand and the W3C-first standards policy). Supersedes the earlier stub.

### Text & Font CSS Properties

Scope: every text/font CSS property and Lynx-specific (`-x-`) text property registered in the C++ engine's property table (`lynx/tools/css_generator/property_index.json`, 236 total properties; enumerated here are the ~36 that are text/font-related), plus the two text-truncation **attributes** (`text-maxline`, `text-maxlength`, distinct from CSS properties). Enum value sets were confirmed in `lynx/core/renderer/css/parser/enum_handler.cc`; the `TextAlignType`/`WhiteSpaceType`/etc. C++ enums themselves are code-generated from the JSON defs at build time and are not checked into this source tree, so enum *names* below are read from the JSON `values[]` arrays and from usages in `enum_handler.cc`, not from a declaration site.

Two engine-wide behavioral facts that affect every row below:
1. **CSS inheritance is off by default.** `lynx/core/renderer/css/dynamic_css_configs.h:33` hardcodes `enable_css_inheritance_ = false`; ReactLynx's build plugin defaults `enableCSSInheritance: false` (`lynx-stack/packages/rspeedy/plugin-react/src/pluginReactLynx.ts:170,370`). When off, **no property inherits at all** — not even `color`/`font-family`/`font-size`. When turned on, only the fixed list in `GetDefaultInheritableProps()` (`lynx/core/renderer/css/dynamic_css_styles_manager.cc:30-38`) inherits: `font-size, font-family, text-align, line-spacing, letter-spacing, line-height, font-style, font-weight, color, text-decoration, text-shadow, direction, cursor` (extensible via `customCSSInheritanceList`). The web target fights the browser's native (always-on) inheritance by forcing `color: initial` on `x-text` (`lynx-stack/packages/web-platform/web-elements/src/elements/XText/x-text.css:12`).
   - **W3C divergence and project decision.** Per CSS2.1/CSS Text, `color`, `font-*`, `line-height`, `text-align`, `white-space`, `word-break`, `letter-spacing`, `direction`, `visibility`, `cursor` etc. are inherited by default; `text-decoration` and `text-shadow` are explicitly **not** inherited (decoration instead paints through descendants via the "propagate" rule). Lynx inherits `text-decoration`/`text-shadow` when inheritance is on, and inherits nothing by default. Per [`docs/style-assumptions.md` §C.9](../style-assumptions.md), lynx-vello intentionally keeps Stylo's full W3C inheritance always on and does not emulate the native `enableCSSInheritance` gate. `stylo_dom::layout::DomLayoutSource` consumes those inherited computed styles directly.
2. **`direction: lynx-rtl` physically mirrors, standard `rtl` does not.** See row below — flag this prominently since it changes `left`/`right`/margin/padding/border/radius resolution, not just text flow.

Implementation status (CSS Text subset, not a complete conformance claim):
`neutron-star::text` implements whitespace processing,
Parley's Unicode bidi shaping, per-run computed `white-space` and `word-break`
overrides promoted through transparent/`display: contents` elements, line
breaking, intrinsic and height-for-width measurement, baselines, and retained
Parley layouts. The current Parley API still chooses an automatic/first-strong
paragraph base level: computed CSS `direction` can resolve physical
`text-align: start`/`end`, but cannot force UAX #9 paragraph ordering. This
limitation is recorded in [`deviations.md`](deviations.md); the implementation
does not inject bidi control characters because that would corrupt source byte
ranges.

`stylo-dom` now stores true Text nodes distinct from Elements: Text has no
external payload, tag, attributes, or computed style, and its `NodeRef`/Stylo
`TNode` behavior reflects that distinction.
`stylo_dom::layout::DomLayoutSource` borrows the Arena, retains only dense
formatting metadata plus computed-style Arcs, and exposes contiguous Text as
an anonymous Parley-measured leaf. `DomLayoutSession` owns the disjoint mutable
text context, artifacts, layouts, and caches; after a commit it exposes rounded
box output and the retained paragraph by real Text node id. Flexbox and Grid
grouping/whitespace omission are W3C rules.

Using that same generated text leaf in Lynx-only Linear and Relative is an
explicit project extension requested so Text participates in all four
algorithms; it is neither W3C behavior nor native-Lynx parity, because native
virtual raw-text is ignored outside the native text-element path.
Lynx PAPI projection, truncation/ellipsis, a general inline formatting context,
inline replaced boxes, decoration painting, and resource fetching remain
separate work.

#### Font properties

| Item | Description | Tier | W3C-compliant? | Deviation & what we should do instead | Source refs |
|---|---|---|---|---|---|
| `font-family` | Comma-separated family list, raw string pass-through to native font resolution (no generic-family keyword remapping observed in the handler) | Core | Partial | Parsed by the generic string handler, not a dedicated font-family grammar handler (no validation of generic keywords like `serif`/`sans-serif`); resolution/fallback happens entirely in the platform's native font manager (Skia/CoreText/etc.), which parley's fallback chain will differ from unless we replicate Lynx's font registration (`lynx/core/renderer/css/css_font_face_token.cc`, `@font-face` support) | `lynx/core/renderer/css/parser/string_handler_unittest.cc:16` (handler assignment); `lynx/core/renderer/css/text_attributes.h:77` |
| `font-size` | Length; also drives `em`/`rpx` computations for descendants when inheritance enabled | Core | Yes | | `lynx/tools/css_generator/css_defines/47-font-size.json`; `lynx/core/renderer/css/text_attributes.h:83` |
| `font-weight` | Enum: `normal, bold, 100..900` (all 9 numeric steps) | Core | Yes | | `lynx/tools/css_generator/css_defines/48-font-weight.json`; `lynx/core/renderer/css/parser/enum_handler.cc:126-149` |
| `font-style` | Enum: `normal, italic, oblique` | Core | Yes | | `lynx/tools/css_generator/css_defines/62-font-style.json`; `lynx/core/renderer/css/parser/enum_handler.cc:246-253` |
| `font-variation-settings` | Variable-font axis settings, passed through as an array to native text shaping | Extended | Yes | Parser exists (`font_variation_settings_handler.cc`) but application depends entirely on native (Skia/CoreText) variable-font support; parley/vello must implement axis application itself | `lynx/core/renderer/css/parser/font_variation_settings_handler.cc/.h`; `lynx/core/renderer/css/text_attributes.h:119` |
| `font-feature-settings` | OpenType feature settings (ligatures, small-caps, etc.) | Extended | Yes | Same caveat as above — native shaping engine dependent | `lynx/core/renderer/css/parser/font_feature_settings_handler.cc/.h`; `lynx/core/renderer/css/text_attributes.h:120` |
| `font-optical-sizing` | Enum: `none, auto` (variable font optical-size axis) | Extended | Yes | | `lynx/tools/css_generator/css_defines/219-font-optical-sizing.json`; `lynx/core/renderer/css/text_attributes.h:121-122` |
| `adapt-font-size` | Legacy predecessor of `-x-auto-font-size`; JSON has empty description, effectively superseded | Rare | No | Non-standard legacy property; treat as deprecated alias, prefer `-x-auto-font-size` semantics | `lynx/tools/css_generator/css_defines/94-adapt-font-size.json` |
| `-x-auto-font-size` | Shrink-to-fit: enable flag + min-size + max-size + step-granularity, shorthand-parsed into a 7-tuple array | Extended | No (non-standard) | No W3C equivalent (closest is CSS `font-size-adjust`, semantically different). Algorithm itself is delegated to an opaque native "textra" paragraph builder (`SetParagraphStyle(kTextPropAutoFontSize,...)`) — exact fitting algorithm not visible in this checkout; must be reverse-engineered behaviorally (bisect within [min,max] by `step_granularity` until text fits container / line count) | `lynx/core/renderer/css/parser/auto_font_size_handler.cc`; `lynx/core/renderer/css/text_attributes.h:73`; `lynx/core/renderer/ui_wrapper/layout/textra/text_layout_textra.cc:397-405` |
| `-x-auto-font-size-preset-sizes` | Explicit candidate font-size list for auto-font-size (alternative to continuous step-granularity search) | Rare | No (non-standard) | | `lynx/tools/css_generator/css_defines/198--x-auto-font-size-preset-sizes.json`; `lynx/core/renderer/css/parser/auto_font_size_preset_sizes_handler.cc/.h` |
| `-x-auto-font-size-line-ranges` | Per-line-range min/max font size overrides (`start_line,end_line,min_size,max_size` tuples) | Rare | No (non-standard) | | `lynx/tools/css_generator/css_defines/226--x-auto-font-size-line-ranges.json`; `lynx/core/renderer/css/text_attributes.h:56-66,74-75`; `lynx/core/renderer/css/parser/auto_font_size_line_ranges_handler.cc/.h` |
| `enable-font-scaling` (attribute, not CSS) | Whether text responds to platform OS-level accessibility font-scale setting | Rare | No (non-standard) | Not in the CSS property table at all — it's a component/global attribute (`TextAttributes::enable_font_scaling`); no W3C analog (closest is `rem`/`math` units + OS font boosting, which browsers handle transparently). Attribute-level, out of pure-CSS scope but must be modeled since it changes effective `font-size` | `lynx/core/renderer/css/text_attributes.h:37,115` |
| `-x-placeholder-font-family` / `-x-placeholder-font-size` / `-x-placeholder-font-weight` / `-x-placeholder-font-style` / `-x-placeholder-color` | Font styling for `<input>`/`<textarea>` placeholder text only (not general text nodes) | Rare | No (non-standard) | W3C uses the `::placeholder` pseudo-element instead of dedicated properties; scope note: only relevant once `<input>`/`<textarea>` components are implemented, not `<text>` | `lynx/tools/css_generator/css_defines/220--x-placeholder-color.json`,`221`,`222`,`223`,`224` |

#### Text layout & inline properties

| Item | Description | Tier | W3C-compliant? | Deviation & what we should do instead | Source refs |
|---|---|---|---|---|---|
| `color` | Text color | Core | Yes | | `lynx/tools/css_generator/property_index.json` (id 22); `lynx/core/renderer/css/text_attributes.h:94` |
| `line-height` | Complex/length; computed into `computed_line_height` (absolute) + `line_height_factor` (multiplier) both stored | Core | Yes | | `lynx/tools/css_generator/css_defines/45-line-height.json`; `lynx/core/renderer/css/text_attributes.h:84-85`; parsed via `FontLengthHandler` (`lynx/core/renderer/css/parser/font_length_handler.cc:39`) supports `normal` keyword |
| `line-spacing` | Extra spacing added between lines, distinct field from `line-height` | Rare | No (non-standard) | Not real CSS; on web target it's passed through as a literal (invalid, no-op) CSS property name (`lynx-stack/packages/web-platform/web-mainthread-apis/dist/style/cssPropertyMap.js:96`, `V('line-spacing')` — browsers ignore unknown props), and in the native "textra" layout-in-element path it is never read by the paragraph builder (only `line-height` is: `text_layout_textra.cc:383-384`) — it's consumed only by legacy Android/iOS native-view text renderers outside this checkout. Treat as **effectively unsupported for the web/vello-style rendering path**; if implemented, add as extra inter-line gap on top of computed line-height, not a line-height replacement | `lynx/tools/css_generator/css_defines/73-line-spacing.json`; `lynx/core/renderer/css/text_attributes.h:87`; `lynx/core/renderer/css/parser/font_length_handler.cc:41`; `lynx/core/renderer/css/computed_css_style.cc:2617-2625` |
| `letter-spacing` | Length, additional space between characters | Core | Yes | | `lynx/tools/css_generator/css_defines/43-letter-spacing.json`; `lynx/core/renderer/css/parser/font_length_handler.cc:40` supports `normal` keyword |
| word-spacing | — | — | N/A | **Does not exist as a Lynx CSS property at all** (no entry in the 236-property table, no handler). parley/CSS-standard word-spacing has no Lynx equivalent to match — safe to support as pure addition, or omit for strict behavioral parity | Absence confirmed via grep across `lynx/tools/css_generator/css_defines/` and `lynx/core/renderer/css/` |
| `text-align` | Enum: `left, center, right, start(v2.0), end(v2.0), justify(v2.10)` | Core | Partial | `start`/`end` resolve through `ResolveTextAlign()` against `direction` (see direction row); `justify` was added late (v2.10) and full justification behavior in the native textra paragraph engine is not visible in this checkout — verify actual justify quality empirically | `lynx/tools/css_generator/css_defines/44-text-align.json`; `lynx/core/renderer/css/dynamic_direction_styles_manager.cc:135-165` (`ResolveTextAlign`) |
| `text-indent` | Length, default `0px`, indents first line only (standard semantics assumed, not independently verified against a layout test) | Core | Yes | | `lynx/tools/css_generator/css_defines/192-text-indent.json`; `lynx/core/renderer/css/text_attributes.h:76` |
| `vertical-align` | Enum: `baseline, sub, super, top, text-top, middle, bottom, text-bottom, center(non-standard extra), length, percent` | Core | Partial | `center` is a Lynx-only addition not in the CSS spec (which has `baseline/sub/super/top/text-top/middle/bottom/text-bottom/<length>/<percentage>`); map `center` to `middle` behavior for compatibility, flag as non-standard alias | `lynx/core/renderer/starlight/style/css_type.h:225-238` (`VerticalAlignType`); `lynx/core/renderer/css/parser/vertical_align_handler.cc:26-54` |
| `white-space` | Enum: **only** `normal, nowrap` | Core | Partial | CSS defines `normal/nowrap/pre/pre-wrap/pre-line/break-spaces`; Lynx supports only 2 of 6 standard keywords — no `pre`/`pre-wrap`/`pre-line` (no verbatim whitespace/newline preservation via this property). Note: `raw-text` web-element hardcodes `white-space-collapse: preserve-breaks` internally for literal `\n` handling (`lynx-stack/packages/web-platform/web-elements/src/elements/XText/x-text.css:252`) — i.e. newline preservation is a raw-text special case, not a general `white-space: pre*` value. lynx-vello should replicate: only `normal`/`nowrap` as user-settable, plus hardcoded newline-preserving behavior inside raw-text runs | `lynx/tools/css_generator/css_defines/42-white-space.json`; `lynx/core/renderer/css/parser/enum_handler.cc:76-89`; `lynx-stack/packages/web-platform/web-elements/src/elements/XText/x-text.css:249-253` |
| `text-overflow` | Enum: `clip` (default), `ellipsis` | Core | Yes | Only 2 values (CSS spec also allows a custom `<string>` — not supported by Lynx, matches subset behavior) | `lynx/tools/css_generator/css_defines/46-text-overflow.json`; `lynx/core/renderer/dom/fiber/text_props.h:60` (`enum class TextOverflow`) |
| `word-break` | Enum: `normal, break-all, keep-all` | Core | Yes | Matches CSS Text Level 3 subset (Lynx omits `break-word`, which is itself a deprecated/non-standard alias in the spec for `overflow-wrap: anywhere` — arguably more spec-correct to omit it) | `lynx/tools/css_generator/css_defines/122-word-break.json`; `lynx/core/renderer/css/parser/enum_handler.cc:373-381` |
| overflow-wrap | — | — | N/A | **Does not exist as a Lynx CSS property.** The web target hardcodes `overflow-wrap: break-word` unconditionally on the `x-text` element itself (not user-configurable) rather than exposing it as a style (`lynx-stack/packages/web-platform/web-elements/src/elements/XText/x-text.css:9`). lynx-vello should hardcode equivalent always-on break-word wrapping behavior for text elements rather than exposing an `overflow-wrap` knob | `lynx-stack/packages/web-platform/web-elements/src/elements/XText/x-text.css:9`; absence confirmed in `lynx/tools/css_generator/css_defines/` |
| `hyphens` | Enum: `none, manual, auto`, default `manual` | Extended | Yes | Default differs from CSS spec default (`manual` here vs. CSS's `manual` — actually matches); verify native hyphenation dictionary availability, likely a native-platform passthrough not implemented at all in the textra path shown here | `lynx/tools/css_generator/css_defines/209-hyphens.json`; `lynx/core/renderer/css/text_attributes.h:114` |
| `direction` | Enum: `normal (default), lynx-rtl, rtl, ltr` | Core | **No** | **W3C divergence (flagged per project instructions).** `direction: lynx-rtl` is a Lynx-only value that *physically* mirrors `left↔right`, `margin-left↔margin-right`, `padding-left↔padding-right`, `border-{left,right}-*`, `border-{top,bottom}-{left,right}-radius`, and `relative-{align,left-of,right-of}` properties (`GetRTLDirectionMapping()`), in addition to affecting text flow. Standard CSS `direction: rtl` only affects logical-property resolution, text/inline flow, and `text-align: start/end`/`justify-content` etc. — it never swaps literal physical `left`/`right` declarations. lynx-vello (stylo) must implement **both**: standard `rtl` (logical-only, matches stylo/W3C default) AND a Lynx-compat mode for `lynx-rtl` that performs the extra physical-property swap Lynx does, gated to only fire when a bundle actually declares `lynx-rtl` (should not touch behavior of standard `rtl`/`ltr`). `text-align: start`/`left`/`right` also resolve differently under `lynx-rtl` vs `rtl` (`ResolveTextAlign`, `lynx/core/renderer/css/dynamic_direction_styles_manager.cc:130-165`) | `lynx/tools/css_generator/css_defines/130-direction.json`; `lynx/core/renderer/css/dynamic_direction_styles_manager.h:24-34`; `lynx/core/renderer/css/dynamic_direction_styles_manager.cc:19-165` (full RTL/logical mapping tables); `lynx/core/renderer/starlight/style/layout_computed_style.h:42-43` |
| writing-mode | — | — | N/A | **No `writing-mode` CSS property exists in Lynx** (no entry in property table, no handler, no enum). Vertical text / vertical writing modes are **not supported** at all in the native engine as far as this checkout shows. Confirm with lynx-stack web team before assuming any web-only vertical-text shim exists; none found | Absence confirmed via grep across `lynx/tools/css_generator/css_defines/` and `lynx/core/renderer/css/` |
| RTL/bidi text shaping | Only the `direction` property (block/inline flow direction) is modeled; no evidence of a `unicode-bidi` property in the C++ CSS layer | — | Partial | Parley applies Unicode BiDi (UAX #9), but its current public builder always uses automatic/first-strong paragraph base direction. lynx-vello therefore cannot yet force paragraph ordering from computed CSS `direction`; it only resolves direction-sensitive physical alignment. Unicode formatting characters in content still participate normally. See `deviations.md` for the integration limitation. | Absence confirmed via grep; `lynx/core/renderer/ui_wrapper/layout/textra/text_layout_textra.cc` (opaque paragraph builder is the native bidi sink) |

#### Text decoration & effects

| Item | Description | Tier | W3C-compliant? | Deviation & what we should do instead | Source refs |
|---|---|---|---|---|---|
| `text-decoration` | Shorthand only (no separate `text-decoration-line`/`-style` longhands in the property table besides `-color`/`-thickness`); parsed token-by-token into line/style/color/thickness components, emits an explicit `none` short-circuit | Core | Partial | CSS defines `text-decoration-line`, `text-decoration-style`, `text-decoration-color`, `text-decoration-thickness` as independently settable longhands plus the `text-decoration` shorthand; Lynx exposes only the shorthand for line+style (both packed via `TextDecorationType` bitflags) but *does* expose `text-decoration-color` and `text-decoration-thickness` as standalone longhands (inconsistent subset) | `lynx/core/renderer/css/parser/text_decoration_handler.cc`; `lynx/core/renderer/css/parser/css_string_parser.cc:475-516` (`ParseTextDecoration`); `lynx/core/renderer/starlight/style/css_type.h:194-209` (`TextDecorationType` bitflags: none/underline/line-through/solid/double/dotted/dashed/wavy/color/thickness) |
| `text-decoration-color` | Standalone longhand | Core | Yes | | `lynx/tools/css_generator/css_defines/148-text-decoration-color.json` |
| `text-decoration-thickness` | Standalone longhand, length | Extended | Yes | | `lynx/tools/css_generator/css_defines/234-text-decoration-thickness.json` |
| `-x-text-decoration-width` | Width of dashed-segment/wavy-period for non-solid decoration styles | Rare | No (non-standard) | No W3C equivalent; W3C leaves dash/wavy period rendering to UA discretion. Needed only for pixel-parity with dashed/wavy underlines, low priority | `lynx/tools/css_generator/css_defines/235--x-text-decoration-width.json` |
| `-x-text-decoration-gap` | Gap between dashed segments / dotted dots | Rare | No (non-standard) | Same as above | `lynx/tools/css_generator/css_defines/236--x-text-decoration-gap.json` |
| `text-shadow` | Comma-separated shadow list (`offset-x offset-y blur-radius color`), standard grammar via shared `ParseShadow`/`ParseSingleShadow` (no inset/spread — text-shadow correctly rejects `inset`) | Core | Partial | Parser/model support multiple comma-separated shadows (`InlineVector<ShadowData,1>` in `TextAttributes`), matching CSS. **However**, in the newer "layout-in-element" native code path the textra paragraph builder explicitly has a `// TODO: text shadow` stub (not yet wired up) — actual rendering falls back to per-platform native text-view shadow support (Android/iOS/Harmony shadow nodes) outside this checkout. Confirm rendering support empirically per platform before assuming full W3C multi-shadow support carries through to pixels | `lynx/core/renderer/css/parser/shadow_handler.cc:24,33`; `lynx/core/renderer/css/parser/css_string_parser.cc:3327-3400` (`ParseShadow`/`ParseSingleShadow`, comma list via `ConsumeCommaSeparatedList`); `lynx/core/renderer/css/text_attributes.h:72`; **TODO stub**: `lynx/core/renderer/ui_wrapper/layout/textra/text_layout_textra.cc:287-289` |
| `text-stroke` | Shorthand: `<width> <color>` or `<color> <width>` (either order, per `ParseTextStroke`), or `none` | Extended | Partial | Not a standard CSS property (closest is unprefixed-vs-`-webkit-text-stroke`, a de facto standard but not in the CSS spec proper); Lynx's own grammar is bidirectional-order which is more lenient than `-webkit-text-stroke`'s fixed `width color` order | `lynx/tools/css_generator/css_defines/194-text-stroke.json`; `lynx/core/renderer/css/parser/css_string_parser.cc:3503-3538` (`ParseTextStroke`); `lynx/core/renderer/css/parser/text_stroke_handler.h` |
| `text-stroke-width` | Standalone longhand | Extended | Partial | Same non-standard-property caveat as `text-stroke` | `lynx/tools/css_generator/css_defines/195-text-stroke-width.json`; `lynx/core/renderer/css/text_attributes.h:88` |
| `text-stroke-color` | Standalone longhand | Extended | Partial | Same caveat | `lynx/tools/css_generator/css_defines/196-text-stroke-color.json`; `lynx/core/renderer/css/text_attributes.h:93` |

#### Line-clamp / truncation (attributes, not CSS properties)

| Item | Description | Tier | W3C-compliant? | Deviation & what we should do instead | Source refs |
|---|---|---|---|---|---|
| `text-maxline` | **Element attribute** (not a CSS property — absent from the 236-entry property table), sets max visible lines before truncation; native side stores as `TextProps::text_max_line` (`std::optional<int>`) and forwards to the opaque textra paragraph builder via `SetParagraphStyle(kTextPropTextMaxLine,...)` | Core | No (non-standard mechanism) | Closest W3C concept is `-webkit-line-clamp` (still non-standard but widely implemented) combined with `display: -webkit-box`. Lynx's own truncation algorithm is opaque/native on this path; **the web-platform reference implementation (`lynx-stack`) shows the actual algorithm concretely** and should be used as the ground truth for a parley-based reimplementation (see below) | `lynx/core/renderer/dom/fiber/text_element.cc:163-172` (attribute set/reset), `:371-374` (forwarded to paragraph builder); `lynx/core/renderer/dom/fiber/text_props.h:43,67`; property absent from `lynx/tools/css_generator/property_index.json` |
| `text-maxlength` | **Element attribute**, max visible UTF-16 character count before truncation (independent of/combinable with `text-maxline`; effective truncation point is `min(maxLengthEndAt, maxLineEndAt)`) | Core | No (non-standard) | No W3C equivalent at all (character-count-based truncation is not a CSS concept) | `lynx-stack/packages/web-platform/web-elements/src/elements/XText/XTextTruncation.ts:129-191` (`#layoutTextInner`, `maxLengthMeasureResult`/`truncateAt = Math.min(maxLengthEndAt, maxLineEndAt)`) |
| Truncation algorithm (web reference impl) | Two-mode algorithm: (1) fast path — if no custom `inline-truncation` child and `tail-color-convert` is not `"false"`, uses native CSS `-webkit-line-clamp`/`text-overflow:ellipsis` (cheap, browser-native); (2) slow path — if a custom `<inline-truncation>` child exists, or `tail-color-convert="false"` forces in-place ellipsis text, does expensive JS-side line/character measurement via `Range.getClientRects()` binary search to find exact truncation offset, then physically truncates DOM text nodes and appends a literal `"..."` text node (`ellipsisLength` = 3, or fewer if last line is short) | Core | No (bespoke) | This is the concrete algorithm lynx-vello's parley-based text layout must replicate for pixel/behavior parity: after normal line-breaking, walk lines up to `text-maxline`; if an inline custom truncation node exists, binary-search the last line for the character offset where `remaining_width < truncation_node_width`, else simply chop the last visible line 3 chars early (or fewer if line is short) and append literal ellipsis dots; also independently clamp to `text-maxlength` UTF-16 chars and take the earlier of the two cut points. Also emits a `layout` event exposing `lineCount` and per-line `{start,end,ellipsisCount}` — lynx-vello should expose equivalent introspection for ReactLynx event-handler compatibility | `lynx-stack/packages/web-platform/web-elements/src/elements/XText/XTextTruncation.ts:56-61` (`#doExpensiveLineLayoutCalculation`), `:139-243` (core algorithm), `:319-375` (`layout` event with `lineCount`/`lines[i].start/end/ellipsisCount`); CSS fast-path: `lynx-stack/packages/web-platform/web-elements/src/elements/XText/x-text.css:207-247` (`-webkit-box`/`-webkit-line-clamp` usage, `:has(> inline-truncation)` feature-detection) |
| `-webkit-line-clamp` (as used internally on web target) | Applied programmatically by `XTextTruncation` (`this.#getInnerBox().style.webkitLineClamp = maxLine`) — not exposed as a Lynx-authorable CSS value, purely an internal implementation detail of the `text-maxline` attribute on web | — | N/A (internal impl) | Not a Lynx-facing property; do not expose to bundle authors. Included here only because it's the concrete mechanism the web reference renderer uses and clarifies what lynx-vello must reproduce without relying on a browser | `lynx-stack/packages/web-platform/web-elements/src/elements/XText/XTextTruncation.ts:306-310` |

#### Not found in Lynx at all (confirmed absent, for explicit scoping)

| Item | Status | Notes |
|---|---|---|
| `word-spacing` | Absent | No property-table entry, no handler, no JSON def anywhere under `lynx/tools/css_generator/css_defines/`. |
| `overflow-wrap` | Absent as a property | Hardcoded always-on `break-word` behavior baked into the `x-text` web element instead of exposed as a style; see table above. |
| `writing-mode` | Absent | No vertical-writing-mode support found anywhere in `lynx/core/renderer/css`. |
| `unicode-bidi` | Absent | Only the coarse `direction` enum exists; no bidi-algorithm control property. |
| `-webkit-line-clamp` / `line-clamp` as author-facing CSS | Absent | Truncation is attribute-driven (`text-maxline`/`text-maxlength`), not CSS-driven, in both native and web targets. |
| `text-decoration-line` (standalone longhand) | Absent | Only available packed inside the `text-decoration` shorthand parse; no separate property ID. |
| `font-kerning`, `font-variant*`, `font-stretch` | Absent | No entries found in the 236-property table under any of these names. |

### Coverage note
The C++ enum *definitions* for `TextAlignType`, `WhiteSpaceType`, `WordBreakType`, `FontWeightType`, `FontStyleType`, `HyphensType`, `FontOpticalSizingType`, `VerticalAlignType` (partially — found in `css_type.h`) are generated at build time from the `tools/css_generator/css_defines/*.json` sources and are **not present as literal enum declarations** in this checkout for most of them (only `VerticalAlignType`, `TextDecorationType`, `JustifyType`, `AnimationDirectionType` were found declared in `core/renderer/starlight/style/css_type.h`); their value sets were instead confirmed via `enum_handler.cc` (which references every enumerator) and the JSON `values[]` arrays, both cited per-row above. The exact truncation/auto-font-size *algorithms* used by the native "textra" paragraph-builder engine are opaque (external/proprietary library, not in this checkout) — only the web-platform TypeScript reference implementation (`XTextTruncation.ts`) gives a fully inspectable algorithm, and is flagged above as the best available ground truth for parley compatibility work.

---

## Also see

Scope note: this is the spec for the `parley` integration — see `.claude/agents/lynx-text-engine.md`.
