{
  "$schema": "https://www.schemastore.org/tsconfig",
  // Three programs, one per environment the code runs in: the page (DOM), its
  // Service Worker (WebWorker) and the Rsbuild config (Node). The DOM and
  // WebWorker libraries cannot share one program.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.worker.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
