{
  "$schema": "https://www.schemastore.org/tsconfig",
  // The QuickJS sources and the Rstest program that runs them on Node: two
  // environments, so two programs.
  "files": [],
  "references": [{ "path": "src" }, { "path": "test" }]
}
