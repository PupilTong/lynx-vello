{
  "name": "bobcat-element",
  "version": "0.0.0",
  "private": true,
  "description": "Bobcat's preloaded QuickJS ESMs: Element PAPI and MTS runtime, Worker and BTS environments, shared timers, EventTarget and cross-thread Contexts.",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.ts",
    "test": "rstest run",
    "test:type": "tsc -b"
  },
  "devDependencies": {
    "@rstest/core": "0.11.8",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
