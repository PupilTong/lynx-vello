[package]
name = "neutron-star"
description = "Trait-first, statically-dispatched CSS flexbox engine and Grid protocol for host-owned trees"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
keywords = ["layout", "flexbox", "grid", "css", "ui"]
categories = ["gui"]

# Deliberately zero dependencies and zero feature flags: the whole engine
# boundary is traits + plain-old-data, so the crate stays
# standalone-publishable and hosts pay for nothing they don't call.
[dependencies]

[lints]
workspace = true
